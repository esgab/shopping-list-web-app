{
  "name": "shopping-list-web-app",
  "version": "1.0.0",
  "description": "Web application for creating shopping lists",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node src/index.js",
    "dev": "npx nodemon src/index.js",
    "publish": "cd web && npm run build && cd .. && rm -rf public ; mv web/dist public"
  },
  "author": "Esther García",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.4.0"
  }
}
